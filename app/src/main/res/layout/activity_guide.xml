<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_guid_viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </android.support.v4.view.ViewPager>
    <!-- 点的容器 -->
    <FrameLayout
        android:id="@+id/fl_guid_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp">

        <LinearLayout
            android:id="@+id/ll_guid_graypoint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="horizontal">
        </LinearLayout>
        <!-- Redpoint-->
        <View
            android:id="@+id/v_guide_redpoint"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/circle_red"/>
    </FrameLayout>

    <TextView
        android:id="@+id/et_guid_exp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:clickable="true"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/fl_guid_container"
        android:text="开始体验"
        android:textColor="#fff"
        style="@style/Padding_10_15"
        android:background="@drawable/bt_color_primary"
        android:textSize="18sp"
        android:visibility="gone"/>


</RelativeLayout>
